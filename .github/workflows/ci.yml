name: School Enquiry Portal CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Verify Repository
        run: echo "School Enquiry Portal CI is working!"
